# Create venv
py venv churn.venv

# Activate (Windows)
source .venv\Scripts\activate


#figsize for subplots
fig,ax = plt.subplots(1,2, figsize=(14,5))

Get-ChildItem -Path C:\ -Filter "churnapp.py" -Recurse

streamlit run .\churnapp\app.py